# NODE_ENV=production is set at runtime (needed for secure cookies, see backend/src/middleware/auth.ts),
# but npm defaults to omitting devDependencies whenever NODE_ENV=production is set during `npm install` -
# which breaks the TypeScript build step (tsc, @types/node, tsx are all devDependencies). Force them in.
include=dev
